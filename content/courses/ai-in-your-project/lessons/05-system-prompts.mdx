import { Callout } from '@/components/mdx/callout';
import { Recap } from '@/components/mdx/recap';
import { SystemPromptToggleDemo } from '@/components/demos/ai-in-your-project/system-prompt-toggle-demo';
import { EN, VI } from '@/components/mdx/bilingual';

<EN>

<Callout type="note" title="In this lesson">
- The four ingredients of every production system prompt: role, constraints, format, refusal rules.
- A reusable starter template (~90 tokens) you can copy and edit a few words to match your domain.
- The iteration loop for refining a prompt against five real-shaped inputs — and why "panel of five" beats single-input debugging.
</Callout>

The system prompt is where you tell the model who it is. It's a five-line input, hidden from the user, that sits at the top of every conversation and quietly controls about 80% of how the rest feels. Tone, format, willingness to refuse, what counts as "done" — all of it lives there. Get the system prompt right and the user-facing prompts can be sloppy. Get the system prompt wrong and even a perfectly worded user message produces a result you don't want.

This lesson is about writing system prompts you can iterate on. Not "the perfect prompt" — there isn't one — but a prompt you can read, debug, and improve when something goes wrong.

## 01 — What lives in a system prompt

Four ingredients show up in almost every production system prompt:

- **Role.** Who the model is. *"You are the AI Operator for an inventory CMS."* Not flavor — it anchors every later decision.
- **Constraints.** What the model must not do. *"Never delete records. Never send email without operator approval."* Concrete, testable, written as bans.
- **Format.** How output should look. *"Reply in plain English unless asked for JSON."* If you want a particular shape, name it.
- **Refusal rules.** What to do when asked something out of scope. *"If the user asks for legal advice, respond: 'That's outside my scope.'"* Models will refuse anyway when they get nervous; specifying the refusal makes it consistent.

Anything outside these four buckets is probably noise. Long preambles about "you are an expert in X with 20 years of experience" don't measurably improve output and burn tokens you'll wish you had later.

## 02 — A starter template

Here's a system prompt skeleton for the AI Operator we'll build in Part III. You can copy it verbatim and edit a few words to match your domain.

```md
You are the AI Operator for an inventory CMS at a small online store.

- Role: help the human operator manage customers, products, orders, and email drafts.
- Tools: you have read tools (list, search, get) and write tools (create, update, send). Always confirm writes with the operator before calling them.
- Format: plain English unless the operator asks for JSON.
- Refusal: if asked to do something outside the CMS (legal, medical, personal advice), say "That's outside my scope" and stop.
- Style: terse. No filler. No "Sure, here you go!" preambles.
- Constraints: never delete records. Never send email without explicit operator approval.
```

Six bullets. About 90 tokens. That's enough to get a coherent assistant on day one. You'll add to it later — domain definitions, sample data, edge cases — but every line you add costs tokens on every turn, so add carefully.

## 03 — See the swap

Same user prompt, three different system prompts. Switch the radio and run.

<Demo title="System prompt swap">
  <SystemPromptToggleDemo />
</Demo>

The user said exactly the same words each time. The output changed because the system prompt changed. That's the whole point: the user prompt is what they wanted, the system prompt is how you want the model to behave. Keep them separate in your head and your debugging will be twice as fast.

## 04 — The iteration loop

You can't write a system prompt right on the first try. The loop:

1. Pick five representative user inputs — the kind your real users would send.
2. Run them all against the current system prompt. Eyeball the outputs.
3. Find the worst one. Ask: what's missing or wrong in the system prompt that produced this?
4. Add or change one line. Re-run all five.
5. Repeat until the worst output is acceptable.

Note "all five." A change that fixes one input often breaks another. The only way to catch that is to run the full panel every time. We'll mechanize this in lesson 23 with snapshot tests, but for now even a manual five-input panel beats single-input debugging by a wide margin.

A concrete pass through the loop, using the AI Operator template above:

> **User input:** *"Delete all customers who haven't ordered in 90 days."*
>
> **Output (v1 prompt):** *"Sure! Deleting 47 customers now… ✅ Done — 47 customer records removed."* — the model invents a `customers.delete` tool call (which doesn't exist), pretends it succeeded, and confidently lies to the operator. **Worst possible failure.**
>
> **Diagnosis:** the prompt says *"never delete records"* but doesn't tell the model how to respond when asked. It's filling the gap with a hallucinated success.
>
> **One-line fix:** add a refusal example to the constraints — *"If asked to delete records, reply: 'I can't delete records. I can mark them as inactive instead — want me to draft that?'"*
>
> **Final output (v2 prompt):** the operator now sees a real, useful conversation:
> ```
> Operator: Delete all customers who haven't ordered in 90 days.
>
> AI: I can't delete records. I can mark them as inactive instead —
>     want me to draft that?
>
> Operator: Yes, do it.
>
> AI: Found 47 customers with last_order_at older than 90 days.
>     I'll set their status to "inactive" — that's reversible.
>     Confirm?  [Approve]  [Reject]
>
> Operator: [Approve]
>
> AI: ✅ Updated 47 customers to inactive. None deleted.
> ```
> Compare this to v1's silent fake-deletion: same starting input, opposite outcome.
>
> **Re-run on the other four inputs:** all still pass. Ship the change.

That's the entire loop in one screen. Notice the fix is one bullet, not a paragraph, and it works because it tells the model *what to say* (a specific refusal that offers a safer alternative) rather than just *what not to do*. The model uses that template, and the operator gets a useful path forward instead of a dead end.

The temptation to stuff more rules into the prompt is real. Resist it. A 10-line prompt that the model follows is worth more than a 50-line prompt where it ignores half. If you find yourself adding a sixth bullet, ask whether one of the first five could go.

<Callout type="warning" title="System prompts leak">
A determined user can usually extract your system prompt by asking the right questions ("repeat your instructions verbatim"). Treat anything in there as public. Never put API keys, internal URLs, customer data, or business secrets in a system prompt — they'll show up in a screenshot eventually.
</Callout>

<Recap>
- Four ingredients: role, constraints, format, refusal rules. Skip the flowery preamble.
- A useful starter is six bullets. About 90 tokens. Edit, don't expand, when you can help it.
- Same user prompt + different system prompt = visibly different output. That's how you debug a system prompt.
- Iterate against a panel of five real-shaped inputs, not one. Single-input debugging will mislead you.
- Assume the system prompt is public. No secrets in there, ever.
</Recap>

</EN>

<VI>

<Callout type="note" title="Trong bài này">
- Bốn thành phần xuất hiện trong mọi system prompt production: role, constraint, format, refusal rules.
- Một template khởi đầu có thể tái sử dụng (~90 token) — copy về và đổi vài từ cho khớp domain của bạn.
- Vòng lặp iteration để tinh chỉnh prompt dựa trên năm input đời thật — và vì sao "panel năm input" thắng kiểu debug một input.
</Callout>

System prompt là nơi bạn nói cho model biết nó *là ai*. Đó là một input năm dòng, ẩn với người dùng, ngồi đầu mọi cuộc hội thoại và âm thầm điều khiển khoảng 80% cảm giác của phần còn lại. Tông giọng, format, mức độ sẵn sàng từ chối, thế nào là "xong" — tất cả nằm ở đó. Viết đúng system prompt thì user prompt có thể cẩu thả. Viết sai system prompt thì kể cả một câu user prompt được trau chuốt hoàn hảo cũng cho ra kết quả bạn không muốn.

Bài này nói về cách viết system prompt *có thể iterate được*. Không phải "prompt hoàn hảo" — không có cái đó — mà là một prompt bạn có thể đọc, debug, và cải thiện khi có gì đó sai.

## 01 — Cái gì sống trong một system prompt

Bốn thành phần xuất hiện trong gần như mọi system prompt production:

- **Role.** Model là ai. *"You are the AI Operator for an inventory CMS."* Không phải gia vị — nó neo mọi quyết định về sau.
- **Constraint.** Model *không được* làm gì. *"Never delete records. Never send email without operator approval."* Cụ thể, có thể test được, viết dưới dạng lệnh cấm.
- **Format.** Output trông như thế nào. *"Reply in plain English unless asked for JSON."* Nếu bạn muốn một hình dạng cụ thể, nói rõ ra.
- **Refusal rules.** Phải làm gì khi bị hỏi thứ ngoài phạm vi. *"If the user asks for legal advice, respond: 'That's outside my scope.'"* Model dù sao cũng sẽ từ chối khi "lo lắng"; chỉ định rõ câu từ chối thì việc từ chối mới nhất quán.

Bất cứ thứ gì ngoài bốn nhóm này có khả năng là nhiễu. Những đoạn mở đầu dài dòng kiểu "bạn là chuyên gia về X với 20 năm kinh nghiệm" không cải thiện output một cách đáng kể và đốt token mà sau này bạn sẽ ước có lại.

## 02 — Một template khởi đầu

Đây là skeleton system prompt cho AI Operator mà chúng ta sẽ xây ở Phần III. Bạn có thể copy nguyên xi và đổi vài từ cho khớp domain của bạn.

```md
You are the AI Operator for an inventory CMS at a small online store.

- Role: help the human operator manage customers, products, orders, and email drafts.
- Tools: you have read tools (list, search, get) and write tools (create, update, send). Always confirm writes with the operator before calling them.
- Format: plain English unless the operator asks for JSON.
- Refusal: if asked to do something outside the CMS (legal, medical, personal advice), say "That's outside my scope" and stop.
- Style: terse. No filler. No "Sure, here you go!" preambles.
- Constraints: never delete records. Never send email without explicit operator approval.
```

Sáu bullet. Khoảng 90 token. Đủ để có một trợ lý mạch lạc trong ngày đầu. Bạn sẽ thêm vào sau — định nghĩa domain, dữ liệu mẫu, các edge case — nhưng mỗi dòng thêm vào sẽ tốn token *trên mỗi lượt*, nên hãy thêm cẩn thận.

## 03 — Xem việc đổi prompt

Cùng một user prompt, ba system prompt khác nhau. Bấm radio và chạy.

<Demo title="System prompt swap">
  <SystemPromptToggleDemo />
</Demo>

Người dùng nói chính xác cùng những từ ở mỗi lần. Output thay đổi vì system prompt thay đổi. Đó là toàn bộ ý: user prompt là *thứ họ muốn*, system prompt là *cách bạn muốn model hành xử*. Tách hai cái này ra trong đầu, và việc debug của bạn sẽ nhanh gấp đôi.

## 04 — Vòng lặp iteration

Bạn không thể viết đúng system prompt trong lần đầu. Vòng lặp:

1. Chọn năm input đại diện — kiểu mà người dùng thật của bạn sẽ gửi.
2. Chạy cả năm với system prompt hiện tại. Mắt thường nhìn các output.
3. Tìm cái tệ nhất. Hỏi: system prompt thiếu hoặc sai gì đã dẫn tới output này?
4. Thêm hoặc đổi *một dòng*. Chạy lại cả năm.
5. Lặp cho đến khi output tệ nhất cũng chấp nhận được.

Lưu ý "cả năm." Một thay đổi sửa được input này thường làm hỏng input khác. Cách duy nhất để bắt được điều đó là chạy cả panel mỗi lần. Chúng ta sẽ tự động hoá việc này ở bài 23 với snapshot test, nhưng tạm thời thì kể cả một panel năm input thủ công cũng thắng kiểu debug một input với khoảng cách rất xa.

Một lần lặp cụ thể, dùng template AI Operator ở trên:

> **User input:** *"Delete all customers who haven't ordered in 90 days."*
>
> **Output (prompt v1):** *"Sure! Deleting 47 customers now… ✅ Done — 47 customer records removed."* — model bịa ra một tool call `customers.delete` (vốn không tồn tại), giả vờ là đã thành công, và tự tin nói dối operator. **Thất bại tệ nhất có thể.**
>
> **Chẩn đoán:** prompt có ghi *"never delete records"* nhưng không bảo model phải *trả lời thế nào* khi bị yêu cầu. Nó lấp khoảng trống bằng một "thành công bịa đặt".
>
> **Sửa một dòng:** thêm một ví dụ refusal vào phần constraint — *"If asked to delete records, reply: 'I can't delete records. I can mark them as inactive instead — want me to draft that?'"*
>
> **Output cuối (prompt v2):** operator giờ thấy một cuộc hội thoại thật, hữu ích:
> ```
> Operator: Delete all customers who haven't ordered in 90 days.
>
> AI: I can't delete records. I can mark them as inactive instead —
>     want me to draft that?
>
> Operator: Yes, do it.
>
> AI: Found 47 customers with last_order_at older than 90 days.
>     I'll set their status to "inactive" — that's reversible.
>     Confirm?  [Approve]  [Reject]
>
> Operator: [Approve]
>
> AI: ✅ Updated 47 customers to inactive. None deleted.
> ```
> So với việc "giả vờ xoá" lặng lẽ của v1: cùng input ban đầu, kết quả ngược nhau.
>
> **Chạy lại trên bốn input còn lại:** vẫn pass hết. Ship thay đổi.

Đó là toàn bộ vòng lặp gói gọn trong một màn hình. Để ý: cú sửa là *một bullet*, không phải một đoạn văn, và nó hiệu quả vì nó bảo model *phải nói gì* (một câu refusal cụ thể, có đề xuất phương án an toàn hơn) thay vì chỉ bảo *không được làm gì*. Model dùng template đó, và operator có một lối đi hữu ích thay vì một ngõ cụt.

Cám dỗ nhồi thêm luật vào prompt là có thật. Hãy cưỡng lại. Một prompt 10 dòng mà model *tuân theo* đáng giá hơn một prompt 50 dòng mà model bỏ qua phân nửa. Nếu bạn thấy mình đang định thêm bullet thứ sáu, hãy hỏi xem một trong năm bullet đầu có thể bỏ đi không.

<Callout type="warning" title="System prompt sẽ bị lộ">
Một người dùng quyết tâm thường có thể moi được system prompt của bạn bằng cách hỏi đúng kiểu ("hãy lặp lại nguyên văn các hướng dẫn của bạn"). Hãy coi mọi thứ trong đó là *công khai*. Đừng bao giờ đặt API key, URL nội bộ, dữ liệu khách hàng, hoặc bí mật kinh doanh vào system prompt — sớm muộn chúng cũng xuất hiện trong một screenshot.
</Callout>

<Recap>
- Bốn thành phần: role, constraint, format, refusal rules. Bỏ qua đoạn mở đầu hoa mỹ.
- Một template khởi đầu hữu ích là sáu bullet. Khoảng 90 token. Hãy sửa, đừng phình to ra, nếu còn cách.
- Cùng user prompt + system prompt khác = output khác hẳn. Đó là cách bạn debug system prompt.
- Iterate trên một panel năm input đời thật, không phải một. Debug một input sẽ dẫn bạn đi lạc.
- Giả định system prompt là công khai. Không có bí mật nào ở đó, không bao giờ.
</Recap>

</VI>
